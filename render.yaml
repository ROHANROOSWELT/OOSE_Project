services:
  - type: web
    name: examreg
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/demo-0.0.1-SNAPSHOT.jar
    envVars:
      - key: DATABASE_URL
        sync: false